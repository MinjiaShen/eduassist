# ── 核心依赖（必装）──────────────────────────────────
flask>=3.0,<4.0
flask-cors>=4.0,<5.0
pyyaml>=6.0
python-docx>=1.1.0
jinja2>=3.1
python-dotenv>=1.0
markdown>=3.5

# ── 可选功能（按需安装）──────────────────────────────
# 语音转录：pip install openai-whisper
# 本地 OCR：pip install paddlepaddle paddleocr
# Claude Vision：pip install anthropic
# HEIC 图片：pip install pillow-heif
